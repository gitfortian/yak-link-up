<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.link.up</groupId>
        <artifactId>link-up</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>link-up-dist</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <dependencies>
        <dependency>
            <groupId>com.link.up</groupId>
            <artifactId>link-up-launcher</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>com.link.up</groupId>
            <artifactId>link-up-server</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
            Reactor ordering only. These must never become runtime dependencies of link-up-dist,
            otherwise Maven would resolve ES7 and ES8 through one flattened dependency graph and
            mediate the incompatible elasticsearch-rest-client versions before classloader isolation.
        -->
        <dependency>
            <groupId>com.link.up</groupId>
            <artifactId>link-up-connector-elasticsearch7</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>com.link.up</groupId>
            <artifactId>link-up-connector-elasticsearch8</artifactId>
            <version>${project.version}</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>${maven-assembly-plugin.version}</version>
                <configuration>
                    <descriptors>
                        <descriptor>src/main/assembly/distribution.xml</descriptor>
                    </descriptors>
                    <finalName>link-up-${project.version}</finalName>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <id>make-distribution</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
